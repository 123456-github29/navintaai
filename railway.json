{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "node --import tsx server/index.ts",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
